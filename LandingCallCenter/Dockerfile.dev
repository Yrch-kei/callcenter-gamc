FROM node:20-alpine

WORKDIR /app

# Copy package manifests and install dependencies
COPY package*.json ./
RUN npm install

# Copy source files
COPY . .

EXPOSE 5173

# Run Vite dev server bound to all network interfaces for container access
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
